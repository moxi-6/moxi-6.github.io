# Linux 常用命令

## 文件与目录

```bash
pwd
ls -la
cd path/to/project
mkdir new-directory
cp source target
mv old new
rm file
```

## 查找内容

```bash
grep -n "关键字" 文件名
find . -name "*.v"
```

## 查看程序位置

```bash
which mdbook
command -v codex
```
